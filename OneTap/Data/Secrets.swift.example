// TEMPLATE — copy this file to "Secrets.swift" (same folder) and paste your real key.
// Secrets.swift is gitignored and must never be committed. This .example file is safe to commit.
import Foundation

enum Secrets {
    static let backendAPIKey = "PASTE_YOUR_BACKEND_API_KEY_HERE"
}
